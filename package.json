{
    "name": "sheet-music--source",
    "version": "0.0.0",
    "scripts": {
        "build": "npm run clean && tsc",
        "clean": "rimraf package/lib",
        "generate-documentation": "rimraf documentation/api && typedoc src",
        "start": "npm run clean && tsc -w",
        "test": "npm run build && tsc --project tsconfig.test.json && mocha test/**/*.js"
    },
    "dependencies": {
        "eventemitter3": "^4.0.7",
        "fraction.js": "^4.0.13",
        "logical-not": "^1.0.7"
    },
    "devDependencies": {
        "@types/mocha": "^8.2.0",
        "@types/node": "^14.14.20",
        "arg": "^5.0.0",
        "mocha": "^8.2.1",
        "readline-sync": "^1.4.10",
        "rimraf": "^3.0.2",
        "shelljs": "^0.8.4",
        "typedoc": "^0.20.14",
        "typescript": "^4.1.3"
    }
}
